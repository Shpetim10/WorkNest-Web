import { AdminView } from '@/features/auth';

export const metadata = {
  title: 'Admin Profile - WorkNest',
  description: 'Set up your administrator account',
};

export default function AdminPage() {
  return <AdminView />;
}
