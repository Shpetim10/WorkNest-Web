import { CompanyView } from '@/features/auth';

export const metadata = {
  title: 'Company Setup - WorkNest',
  description: 'Tell us about your organization',
};

export default function CompanySetupPage() {
  return <CompanyView />;
}
