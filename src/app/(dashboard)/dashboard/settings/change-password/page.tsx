import { ChangePasswordView } from "@/features/dashboard";

export const metadata = {
  title: 'Change Password | WorkNest',
};

export default function ChangePasswordPage() {
  return <ChangePasswordView />;
}
