import { CompanySettingsView } from "@/features/dashboard";

export const metadata = {
  title: 'Company Settings | WorkNest',
};

export default function CompanySettingsPage() {
  return <CompanySettingsView />;
}
