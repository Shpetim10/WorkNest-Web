import { CompaniesManagementView } from '@/features/super-admin';

export const metadata = {
  title: 'Companies Management | WorkNest',
};

export default function CompaniesManagementPage() {
  return <CompaniesManagementView />;
}
